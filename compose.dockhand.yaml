services:
  estoualer:
    image: php:8.5.2-apache
    container_name: estoualer
    restart: unless-stopped
    ports:
      - 4201:80
    volumes:
      - ./src:/var/www/html:ro,z
      - ./php.ini-production:/usr/local/etc/php/php.ini:ro,z

networks:
  default:
    name: estoualer
